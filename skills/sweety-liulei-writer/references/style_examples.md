# liulei 配对示例库（私有，本仓库仅存根）

本文件的完整内容是"AI 通用腔 vs 真实原文"的配对手法库（手法 1–19 + 真实写法图谱 + 缺口记录 + 实测校准），**含大量语料原文，不入公开仓库**。

- 完整版位置（本机私有）：`~/.codex/writing-style/liulei/private-refs/style_examples.md`
- **执行本 skill 时必读完整版**：写作或改稿前，从上述本地路径读取；公开仓库不保留任何原文与引用。
- 追加新配对（§9 学习迭代）也写入本地完整版，不写回本存根。
- 若本地文件缺失：按 SKILL.md 第 1、2、6 节的人格锚、价值观和红线执行（弱匹配兜底规则），并提示用户恢复私有档案。
